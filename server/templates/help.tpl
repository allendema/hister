{{define "main"}}
<div class="container">
<h2>Search shortcuts</h2>
<p>Press <kbd>enter</kbd> to open the first result in a new tab</p>
<p>Navigate in results with <kbd>ctrl+j</kbd> and <kbd>ctrl+k</kbd>. <kbd>Enter</kbd> opens the selected result in a new tab.</p>
<p>Press <kbd>ctrl+o</kbd> to open the search query in the configured search engine on a new tab.</p>
</div>
{{end}}
